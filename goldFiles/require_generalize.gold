
#include <assert.h>
#include <cstring>
 
using namespace std;
 
// Global variables defining starting state
unsigned int n = 4;
unsigned int tmp = 0;
 
 
int main()
{
    
    
    unsigned int i = 0;
    while(i != n)
    {
        if (((i) == (3)) && ((tmp) == (0)))
        {
            i -= 3;
            tmp += 1;
        }
        i += 1;
        assert(!(i == 0));
    }
}
