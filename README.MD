# Bachelor project for static assertion checking in a Janus-like language

* `doc/` contains latex src code for the bachelor project.
* `src/` contains the src code for the project, written in Haskell.